let express,getGenres;_519‍.w("express",[["default",["express"],function(v){express=v}]]);_519‍.w("./lib/scraper",[["default",["getGenres"],function(v){getGenres=v}]]);


const app = express();

app.get('/scrape', async (req, res, next) => {
  console.log('Scraping!');
  let hash = await getGenres(
    'http://everynoise.com/everynoise1d.cgi?scope=all'
  );
  res.json({ hash });
});

app.listen(3006, () => {
  console.log(`running on port 3006`);
});
